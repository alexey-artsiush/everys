import React from "react";
import classes from "./Footer.module.scss";

function Footer() {
  return (
    <footer className={classes.footer}>
      <h3 className={classes.tagline}>Слоган</h3>
    </footer>
  );
}

export default Footer;
